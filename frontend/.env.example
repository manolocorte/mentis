# Base URL for the Mentis API (no trailing slash)
# Defaults to http://localhost:8000 when not set
VITE_API_BASE_URL=https://your-api-gateway-id.execute-api.us-east-1.amazonaws.com/prod

# Optional API key sent as x-api-key header on every request
VITE_API_KEY=
